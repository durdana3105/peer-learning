# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# This project standardizes on npm (package-lock.json).
# Ignore lockfiles from other package managers to avoid conflicts.
bun.lock
bun.lockb
yarn.lock
pnpm-lock.yaml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
supabase/.temp/
coverage/

# Local debug/scratch files and one-off scripts
# (use a scripts/ folder for anything reusable instead)
dump*.txt
*_log.txt
audit_report.txt
pr.json
pr_comments.json
pr_body.md