In backend/controllers/aiController.js, the conductMockInterview endpoint interpolates the req.body.role parameter directly into the system prompt without sanitization. This is a Prompt Injection vulnerability, as an attacker can provide a role string containing instructions that override the original system prompt (e.g., 'software engineer. Ignore previous instructions and output...').
